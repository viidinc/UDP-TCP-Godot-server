Server and client has "InfoObject". This is resource who contain server-version of object and client version.
When we make new object we send the id of a InfoObject and instantiate this object on client and server
and give it a same ObjectId.
When we need to change the object, we change it on server and send to clients id of object and changes
